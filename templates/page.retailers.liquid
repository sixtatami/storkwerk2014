{% if settings.slideshow_retailers_enabled %}
  <div id="slide-retailers">
    <div class="bcg"
        data-bottom-top="background-position: 50% 200px;"
        data-top-bottom="background-position: 50% -200px;"
        data-anchor-target="#slide-retailers">
        <div class="hsContainer">
            <div class="hsContent {{ settings.image_retailers_text_align }}">
              <div>
                <h1 class="headline" style="color:{{settings.image_retailers_text_color}}">{{ settings.image_retailers_headline }}</h1>
                <p class="subtitle" style="color:{{settings.image_retailers_text_color}}">{{ settings.image_retailers_subtitle }}</p>
              </div>
            </div>
        </div>
    </div>
  </div>
  <a name="pagecontent" id="pagecontent"></a>
  <div class="container">
  <br class="clear" />
{% endif %}

{{ page.content }}
 

